import { Request, Response } from "express";



export const login = async (req: Request, res: Response)  => {
    try {
        const foundUser = null; // userService function
        console.log('login')
    } catch (error) {
    }
}


export const register  = async (req: Request, res: Response) => {
    try {
        const register = null; 
        console.log('register')
    } catch (error) {
        
    }
}